[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType  = "ON_FAILURE"
restartPolicyMaxRetries = 5

# No healthcheck needed — this is a polling bot, not a web server.
# Railway will keep it alive as long as the process is running.
